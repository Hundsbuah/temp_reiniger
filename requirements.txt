# Laufzeit-Abhängigkeiten (für Start aus dem Quellcode)
# Die fertige EXE (dist/Temp-Reiniger.exe) ist davon unabhängig.
customtkinter>=5.2
pystray>=0.19          # Windows: reines ctypes (Pillow + six werden automatisch mitgezogen), kein pywin32 nötig
Pillow>=10.0

# Nur zum Bauen der EXE:
# pyinstaller>=6.0